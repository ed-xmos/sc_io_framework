constants: {}
name: pipeline
yaml_dict:
  includes:
  - stdint.h
  module:
    pipeline:
      checksum:
        help: The checksum of the pipeline, set at compile time. Used to verify the
          control command and pipeline are in sync.
        size: 16
        type: uint8_t
